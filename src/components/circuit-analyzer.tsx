import { useRef, useState } from "react";
import { AlertTriangle, FileImage, ScanLine, Upload } from "lucide-react";
import { Button } from "@/components/ui/button";
import { PageHeader } from "./fixmate-ui";

export function CircuitAnalyzer() { const input = useRef<HTMLInputElement>(null); const [file,setFile]=useState<File|null>(null); const [drag,setDrag]=useState(false);
  return <><PageHeader eyebrow="Vision module" title="Circuit Analyzer" text="Upload a clear circuit, schematic, or breadboard image to preview detected components and possible issues."/><div className="grid gap-5 pb-16 lg:grid-cols-[1.1fr_.9fr]"><section className="frost rounded-xl p-5 ring-1 ring-border"><div onDragOver={e=>{e.preventDefault();setDrag(true)}} onDragLeave={()=>setDrag(false)} onDrop={e=>{e.preventDefault();setDrag(false);setFile(e.dataTransfer.files[0] ?? null)}} className={`grid min-h-80 place-items-center rounded-lg border border-dashed p-8 text-center transition ${drag ? "border-primary bg-primary/10" : "border-border bg-ink/50"}`}><div><div className="mx-auto grid size-14 place-items-center rounded-xl bg-primary/10 text-signal ring-1 ring-primary/30"><Upload/></div><h2 className="mt-4 font-semibold">{file ? file.name : "Drop a circuit image here"}</h2><p className="mt-2 text-sm text-steel">PNG or JPG · up to 10 MB</p><input ref={input} type="file" accept="image/png,image/jpeg" className="hidden" onChange={e=>setFile(e.target.files?.[0] ?? null)}/><Button className="mt-5" variant="console" onClick={()=>input.current?.click()}><FileImage/> Choose image</Button></div></div></section><aside className="frost rounded-xl p-5 ring-1 ring-border"><div className="flex items-center justify-between"><div><div className="font-mono text-[10px] uppercase text-signal">AI analysis</div><h2 className="mt-1 text-lg font-semibold">{file ? "Mock analysis complete" : "Awaiting image"}</h2></div><ScanLine className="size-5 text-signal"/></div><div className="mt-5 space-y-3">{[["U1","ATmega328P"],["R1–R4","220Ω resistors"],["D1–D2","Red LEDs"],["C1","100nF capacitor"]].map(([a,b])=><div key={a} className="flex justify-between rounded-md bg-secondary/70 px-3 py-2.5 font-mono text-xs ring-1 ring-border"><span className="text-steel">{a}</span><span>{b}</span></div>)}</div><div className="mt-5 rounded-lg bg-accent/10 p-4 ring-1 ring-accent/30"><div className="flex items-center gap-2 font-mono text-[10px] uppercase text-amber"><AlertTriangle className="size-4"/> Possible issue</div><p className="mt-2 text-sm leading-6 text-steel">Potential loose jumper near the ground rail. Verify continuity before powering the board.</p></div><p className="mt-4 font-mono text-[9px] uppercase text-steel">Demo output · not a verified electrical assessment</p></aside></div></>;
}